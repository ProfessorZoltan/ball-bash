{
  "name": "deflector-relay",
  "private": true,
  "type": "module",
  "description": "Deflector's online multiplayer relay, a Cloudflare Worker with one Durable Object",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
